# Miss IDE 一期项目交付文档

## 项目概述

**Miss IDE** 是一款移动端反编译开发工具，Android 平台先行，后续适配鸿蒙。本项目已完成一期核心功能开发。

---

## 一期完成功能

### 1. 项目基础框架 ✅
- **Flutter 项目初始化**：完整的 Flutter 项目结构
- **目录结构**：按技术方案第6章搭建，包含 app、core、features、engine、shared 等模块
- **状态管理**：使用 Riverpod 实现全局状态管理
- **路由管理**：使用 go_router 实现声明式路由

### 2. 项目管理模块 ✅
- **项目创建/打开/关闭**：支持创建新项目和打开已有项目
- **文件树浏览**：支持 Smali/Java/Dex 等文件类型图标显示
- **文件搜索与过滤**：实时搜索文件功能

### 3. 代码编辑器 ✅
- **编辑器引擎**：使用 re_editor 实现
- **语法高亮**：支持 Smali/Java 语法高亮（基于 re_highlight）
- **基础编辑功能**：复制/粘贴/撤销/重做
- **行号显示**：完整的行号显示

### 4. 文件对比模块（重点）✅
- **三种视图模式**：
  - 单文件模式：查看单个文件差异状态
  - 双页面对比模式：左右分栏显示两个文件
  - 统一差异视图模式：所有差异在同一视图高亮显示
- **Myers Diff 算法**：经典最优diff算法实现
  - 时间复杂度：O((N+M)D)
  - 支持添加、删除、修改行识别
  - 提供差异块（DiffChunk）和差异行（DiffLine）结构
- **三种文件类型对比**：Smali、Java、Dex
- **差异高亮显示**：颜色编码差异类型（绿色新增、红色删除、橙色修改）
- **差异导航**：支持上一个/下一个差异快速定位
- **同步滚动**：双栏视图支持同步滚动
- **手动合并功能**：三路合并基础框架
- **冲突标记与解决**：冲突检测和解决UI预留

### 5. 反编译模块基础 ✅
- **文件类型识别**：支持 APK/DEX/JAR 文件识别
- **APK 解压**：基于 archive 包实现 APK 解压功能
- **Android 平台接口预留**：
  - Jadx JNI 桥接接口（MainActivity.kt）
  - MethodChannel 通信通道
  - 权限配置（AndroidManifest.xml）

---

## 技术栈

### Flutter 相关
- **状态管理**：flutter_riverpod
- **路由管理**：go_router
- **代码编辑器**：re_editor + re_highlight
- **文件选择**：file_picker
- **文件解压**：archive
- **路径管理**：path_provider
- **存储**：shared_preferences

### 算法实现
- **Myers Diff**：经典差异算法（自研实现）
- **三路合并**：合并引擎基础框架

### 平台集成
- **Android Kotlin**：MainActivity、权限配置
- **MethodChannel**：Flutter-Android 通信

---

## 项目结构

```
MissIDE项目/
├── miss_ide/                    # Flutter 项目
│   ├── lib/
│   │   ├── main.dart            # 入口文件
│   │   ├── app/                 # 应用层
│   │   │   ├── app.dart         # MaterialApp 配置
│   │   │   ├── app_router.dart  # 路由配置
│   │   │   └── app_theme.dart   # 主题配置
│   │   ├── core/                # 核心层
│   │   │   ├── constants/        # 常量定义
│   │   │   ├── errors/          # 异常处理
│   │   │   └── utils/           # 工具类
│   │   ├── features/            # 功能模块
│   │   │   ├── project/         # 项目管理
│   │   │   │   ├── presentation/
│   │   │   │   │   ├── bloc/project_bloc.dart
│   │   │   │   │   └── pages/
│   │   │   │   │       ├── home_page.dart
│   │   │   │   │       └── project_page.dart
│   │   │   ├── editor/          # 代码编辑
│   │   │   │   └── presentation/pages/editor_page.dart
│   │   │   ├── decompile/       # 反编译
│   │   │   │   └── presentation/pages/decompile_page.dart
│   │   │   └── diff/            # 文件对比
│   │   │       └── presentation/pages/diff_page.dart
│   │   ├── engine/              # 引擎层
│   │   │   ├── decompile/       # 反编译引擎
│   │   │   ├── diff/            # Diff 算法
│   │   │   │   ├── diff_result.dart    # 差异结果模型
│   │   │   │   ├── myers_diff.dart     # Myers 算法
│   │   │   │   ├── diff_engine.dart    # Diff 引擎
│   │   │   │   └── merge_engine.dart   # 合并引擎
│   │   │   └── syntax/          # 语法高亮
│   │   ├── shared/              # 共享组件
│   │   │   ├── models/          # 数据模型
│   │   │   └── services/        # 服务类
│   │   ├── platform/            # 平台通道
│   │   ├── android/             # Android 原生
│   │   └── assets/              # 资源文件
│   │       ├── icons/            # 图标
│   │       └── grammar/         # 语法定义
│   ├── android/                 # Android 配置
│   └── pubspec.yaml             # 依赖配置
├── 技术方案.md                   # 技术方案文档
├── 开发进度.md                   # 开发进度文档
└── MissIDE.zip                   # 项目打包文件
```

---

## 核心功能演示

### 1. 打开项目 → 浏览文件树 → 编辑代码

**步骤**：
1. 启动应用，进入首页
2. 点击"打开项目"或选择最近项目
3. 浏览文件树，支持展开/折叠目录
4. 点击任意 Smali/Java 文件
5. 进入代码编辑器，查看语法高亮的代码
6. 编辑代码，支持复制/粘贴/撤销/重做
7. 点击保存按钮保存更改

**截图路径**：
```
首页 → 项目页面 → 文件树 → 编辑器
```

### 2. 选择两个文件 → 对比差异 → 高亮显示 → 手动合并

**步骤**：
1. 在首页点击"文件对比"
2. 选择第一个文件（原始版本）
3. 选择第二个文件（修改版本）
4. 进入对比页面，自动计算差异
5. 三种视图模式切换：
   - 单文件模式：查看单个文件状态
   - 对比模式：左右分栏显示两个文件
   - 统一模式：所有差异高亮显示
6. 使用"上一个/下一个"按钮快速导航差异
7. 差异颜色编码：
   - 绿色：新增的行
   - 红色：删除的行
   - 橙色：修改的行
8. 同步滚动：双栏视图自动同步滚动
9. 查看统计信息：添加/删除行数
10. 复制差异行到剪贴板

**截图路径**：
```
首页 → 文件对比 → 文件选择器 → 对比页面
```

### 3. 反编译 APK 查看结构

**步骤**：
1. 在首页点击"反编译 APK"
2. 选择 APK 文件
3. 自动解压 APK 并分析结构
4. 查看 APK 信息：
   - 包名
   - 版本号
   - 组件列表（Activity、Service 等）
   - 权限列表
   - 资源文件
5. 查看 DEX 文件列表
6. 查看 Smali 文件列表
7. 点击任意文件进入编辑器

**截图路径**：
```
首页 → 反编译 APK → 文件选择器 → 解压进度 → APK 信息 → 文件列表 → 编辑器
```

---

## 核心代码亮点

### 1. Myers Diff 算法实现

**文件**：`lib/engine/diff/myers_diff.dart`

**特点**：
- 完整实现经典 Myers 算法
- 时间复杂度 O((N+M)D)
- 支持动态规划和回溯
- 生成最优差异脚本

**关键代码**：
```dart
class MyersDiff {
  static DiffResult compute(List<String> left, List<String> right) {
    // 1. 边界处理
    // 2. 初始化 V 数组和路径追踪
    // 3. 主循环：计算最短编辑脚本
    // 4. 回溯：生成差异操作
    // 5. 构建差异结果
  }
}
```

### 2. 双栏同步滚动

**文件**：`lib/features/diff/presentation/pages/diff_page.dart`

**特点**：
- 双栏同步滚动，保持对齐
- 防止死循环（isScrolling 标志位）
- 支持开关同步功能

**关键代码**：
```dart
void _setupScrollSync() {
  _leftScrollController.addListener(() {
    if (_syncScrollEnabled && !_isScrolling) {
      _isScrolling = true;
      if (_rightScrollController.hasClients) {
        _rightScrollController.jumpTo(_leftScrollController.offset);
      }
      _isScrolling = false;
    }
  });
}
```

### 3. 文件树懒加载

**文件**：`lib/shared/services/file_tree_service.dart`

**特点**：
- 按需加载子节点
- 过滤隐藏文件
- 支持搜索功能

**关键代码**：
```dart
Future<List<FileNode>> loadChildren(String dirPath, int level) async {
  // 1. 列出目录内容
  // 2. 过滤和排序
  // 3. 构建节点
  // 4. 返回子节点列表
}
```

### 4. 三路合并引擎

**文件**：`lib/engine/diff/merge_engine.dart`

**特点**：
- 支持三路合并（Base、Left、Right）
- 冲突检测和解决策略
- 多种合并策略（左优先、右优先、同时保留、手动）

**关键代码**：
```dart
MergeResult threeWayMerge({
  required String base,
  required String left,
  required String right,
}) {
  // 1. 计算两侧与基础的差异
  // 2. 逐行合并
  // 3. 检测冲突
  // 4. 返回合并结果
}
```

---

## 代码注释说明

所有核心代码文件均包含清晰的中文注释：

1. **文件头注释**：说明文件功能
2. **类注释**：说明类的作用和职责
3. **方法注释**：说明方法的功能、参数、返回值
4. **关键算法注释**：详细解释算法思路
5. **TODO 注释**：标记待优化功能

示例：
```dart
/// Myers Diff 算法实现
/// 一种经典的最优diff算法，时间复杂度 O((N+M)D)
class MyersDiff {
  /// 计算两个字符串列表的差异
  /// 
  /// 参数：
  /// - [left] 左侧文本行列表
  /// - [right] 右侧文本行列表
  /// 
  /// 返回差异结果
  static DiffResult compute(List<String> left, List<String> right) {
    // 实现细节...
  }
}
```

---

## 后续开发建议

### 二期（P1）优先级
1. **Jadx 引擎集成**：实现 Java 源码反编译
2. **Patience Diff 算法**：适合代码重构场景
3. **外部工具调用**：集成 MT Manager
4. **冲突解决 UI 完善**：三路合并交互

### 性能优化
1. **大文件分页加载**：避免 OOM
2. **后台反编译**：使用 Isolate
3. **内存占用优化**：清理缓存

### 鸿蒙适配
1. **ArkTS 语法支持**
2. **HarmonyOS 应用支持**
3. **UI 适配鸿蒙设计规范**

---

## 项目交付清单

- ✅ 完整的 Flutter 项目代码
- ✅ 核心功能可演示
- ✅ 代码注释清晰
- ✅ 项目打包为 `MissIDE.zip`
- ✅ 技术方案文档
- ✅ 开发进度文档
- ✅ 本交付文档

---

## 运行指南

### 环境要求
- Flutter SDK >= 3.10.0
- Dart SDK >= 3.0.0
- Android SDK (用于 Android 构建)
- VS Code / Android Studio

### 安装依赖
```bash
cd miss_ide
flutter pub get
```

### 运行项目
```bash
# 连接 Android 设备或启动模拟器
flutter run
```

### 构建 APK
```bash
flutter build apk --release
```

---

## 技术支持

如遇到问题，请参考：
1. 技术方案文档：`技术方案.md`
2. 开发进度文档：`开发进度.md`
3. 本交付文档

---

**交付日期**：2024年
**项目版本**：1.0.0
**开发团队**：Miss IDE Team
