# Miss IDE 部署文档

## 一、项目概述

Miss IDE 是一款移动端反编译开发工具，支持 Android APK 反编译、代码编辑、三路合并、冲突解决等功能。

## 二、GitHub 仓库

- **仓库地址**: https://github.com/qq00150610-cpu/miss-ide
- **仓库结构**:
  - `miss_ide/` - Flutter 主应用
  - `miss_admin/` - 管理后台 Web 应用
  - `backend/` - Node.js 后端 API 服务
  - `database/` - 数据库初始化脚本

## 三、阿里云服务器信息

- **服务器IP**: 47.92.220.102
- **SSH端口**: 22
- **用户名**: root

## 四、后端部署

### 4.1 服务器环境准备

```bash
# 安装 Node.js 18
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 安装 PM2
npm install -g pm2

# 安装 Nginx
sudo apt-get install -y nginx

# 安装 MySQL
sudo apt-get install -y mysql-server
```

### 4.2 上传后端代码

```bash
# 在本地打包
cd MissIDE项目/backend
npm install
tar -czvf backend.tar.gz *

# 上传到服务器
scp -P 22 backend.tar.gz root@47.92.220.102:/tmp/

# 在服务器解压
ssh root@47.92.220.102
mkdir -p /var/www/miss-ide
cd /var/www/miss-ide
tar -xzvf /tmp/backend.tar.gz
```

### 4.3 配置环境变量

```bash
cd /var/www/miss-ide
cp .env.example .env
nano .env  # 编辑配置
```

### 4.4 初始化数据库

```bash
mysql -u root -p < /var/www/miss-ide/database/init.sql
# 或使用服务器上的数据库脚本
mysql -u root -p misside2026 < database/init.sql
```

### 4.5 启动服务

```bash
cd /var/www/miss-ide
npm install
pm2 start server.js --name miss-ide-api
pm2 save
pm2 startup  # 设置开机自启
```

### 4.6 Nginx 配置

```bash
sudo nano /etc/nginx/sites-available/miss-ide
```

```nginx
server {
    listen 80;
    server_name 47.92.220.102;

    # 静态文件
    root /var/www/miss-ide/miss_admin/web;
    index index.html;

    # API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
    }

    # 上传文件
    location /uploads/ {
        alias /var/www/miss-ide/uploads/;
        autoindex off;
    }

    # 管理后台
    location /admin/ {
        alias /var/www/miss-ide/miss_admin/web/;
        try_files $uri $uri/ /index.html;
    }

    # 前端应用
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

```bash
sudo ln -s /etc/nginx/sites-available/miss-ide /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

## 五、APK 构建

### 5.1 本地构建 (需要 Flutter SDK)

```bash
cd MissIDE项目/miss_ide

# 生成签名密钥
keytool -genkey -v -keystore miss-ide-key.jks \
  -keyalg RSA -keysize 2048 -validity 10000 \
  -alias miss-ide \
  -storepass misside2026 \
  -keypass misside2026 \
  -dname "CN=Miss IDE, OU=Development, O=Miss IDE Team"

# 创建 key.properties
echo "storePassword=misside2026
keyPassword=misside2026
keyAlias=miss-ide
storeFile=miss-ide-key.jks" > android/key.properties

# 构建 APK
flutter pub get
flutter build apk --release
```

### 5.2 构建产物

- **APK**: `miss_ide/build/app/outputs/flutter-apk/app-release.apk`
- **AAB**: `miss_ide/build/app/outputs/bundle/app-release.aab`

### 5.3 下载地址

构建完成后，APK 可通过以下方式获取：
- 服务器下载: http://47.92.220.102/downloads/miss-ide-v1.0.0.apk
- GitHub Releases: https://github.com/qq00150610-cpu/miss-ide/releases

## 六、服务地址

| 服务 | 地址 |
|------|------|
| 后端 API | http://47.92.220.102:3000 |
| 健康检查 | http://47.92.220.102:3000/health |
| 管理后台 | http://47.92.220.102/admin/ |
| 管理后台 API | http://47.92.220.102/api/admin/ |

## 七、数据库连接

- **类型**: MySQL
- **主机**: 47.92.220.102
- **端口**: 3306
- **数据库名**: miss_ide
- **用户名**: root
- **密码**: (见服务器配置)

## 八、API 接口文档

### 认证接口

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | /api/auth/register/email | 邮箱注册 |
| POST | /api/auth/register/phone | 手机注册 |
| POST | /api/auth/login/email | 邮箱登录 |
| POST | /api/auth/login/phone | 手机登录 |
| GET | /api/auth/me | 获取当前用户 |
| POST | /api/auth/change-password | 修改密码 |
| POST | /api/auth/forgot-password | 忘记密码 |

### 用户接口

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/users/profile | 获取用户资料 |
| PUT | /api/users/profile | 更新用户资料 |
| GET | /api/users/projects | 获取项目列表 |
| POST | /api/users/projects | 创建项目 |
| DELETE | /api/users/projects/:id | 删除项目 |

### 同步接口

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | /api/sync | 同步数据 |
| GET | /api/sync | 获取同步数据 |
| POST | /api/sync/batch | 批量同步 |
| DELETE | /api/sync | 删除同步数据 |

### 管理接口 (需管理员权限)

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/admin/users | 获取用户列表 |
| PUT | /api/admin/users/:id | 更新用户 |
| DELETE | /api/admin/users/:id | 删除用户 |
| GET | /api/admin/logs | 获取日志 |
| GET | /api/admin/stats | 获取统计 |

## 九、运维命令

```bash
# 查看服务状态
pm2 status

# 查看日志
pm2 logs miss-ide-api

# 重启服务
pm2 restart miss-ide-api

# 停止服务
pm2 stop miss-ide-api

# 重载配置
pm2 reload miss-ide-api

# Nginx 日志
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
```

## 十、常见问题

### 1. 数据库连接失败
检查 MySQL 服务状态：`sudo systemctl status mysql`
检查端口是否开放：`sudo ufw allow 3306`

### 2. API 无法访问
检查服务状态：`pm2 status`
检查端口监听：`netstat -tlnp | grep 3000`
检查防火墙：`sudo ufw status`

### 3. 上传文件失败
检查 uploads 目录权限：`sudo chmod 755 /var/www/miss-ide/uploads`

## 十一、安全建议

1. 修改 MySQL root 密码
2. 配置 SSL 证书 (HTTPS)
3. 限制 API 访问频率
4. 定期备份数据库
5. 配置防火墙规则
6. 不要将敏感信息提交到 Git

---

*文档更新时间: 2026-04-18*
