# 鸿蒙适配指南

## 概述

Miss IDE Phase 4 增加了对 HarmonyOS Next 的支持。本文档介绍如何在鸿蒙设备上运行 Miss IDE。

## 平台适配架构

```
┌─────────────────────────────────────────────────────────┐
│                    Miss IDE Flutter App                   │
├─────────────────────────────────────────────────────────┤
│                    Platform Adapter Layer                │
│  ┌─────────────────────────────────────────────────────┐│
│  │              PlatformAdapter (抽象接口)              ││
│  └─────────────────────────────────────────────────────┘│
│                           │                              │
│         ┌─────────────────┼─────────────────┐           │
│         │                 │                 │            │
│  ┌──────▼──────┐  ┌──────▼──────┐  ┌──────▼──────┐     │
│  │  Android    │  │  HarmonyOS  │  │    iOS      │     │
│  │  Adapter    │  │  Adapter    │  │  Adapter    │     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
└─────────────────────────────────────────────────────────┘
```

## 文件结构

```
lib/platform/
├── platform_adapter.dart    # 抽象接口和工厂
├── platform_android.dart    # Android 平台实现
└── platform_ohos.dart      # 鸿蒙平台实现
```

## 核心接口

### PlatformAdapter

平台适配器提供以下核心能力：

| 方法 | 说明 |
|------|------|
| `getPlatformInfo()` | 获取平台信息 |
| `getAppDataDirectory()` | 获取应用数据目录 |
| `getCacheDirectory()` | 获取缓存目录 |
| `hasStoragePermission()` | 检查存储权限 |
| `requestStoragePermission()` | 请求存储权限 |
| `openExternalApp()` | 打开外部应用 |
| `isAppInstalled()` | 检查应用是否安装 |
| `executeShell()` | 执行 Shell 命令 |

## 鸿蒙特定实现

### ArkUI 渲染适配

鸿蒙应用使用 ArkUI 框架进行渲染。对于 Flutter 应用，需要处理：

1. **Flutter Engine 集成**
   - 使用 Flutter 官方对鸿蒙的支持
   - ArkUI 作为原生渲染层

2. **MethodChannel 适配**
   ```dart
   // 鸿蒙使用不同的 Channel 名称
   static const _channel = MethodChannel('com.misside/ohos');
   static const _faChannel = MethodChannel('com.misside/fa');
   ```

### 权限模型

鸿蒙使用不同的权限模型：

```dart
// 检查存储权限
Future<bool> hasStoragePermission() async {
  final result = await _channel.invokeMethod('hasStoragePermission');
  return result as bool? ?? false;
}
```

### FA 模型（Feature Ability）

鸿蒙应用的 FA 模型与 Android Intent 类似：

```dart
// 启动其他应用
Future<bool> openExternalApp(String bundleName, {Map<String, dynamic>? data}) async {
  final result = await _faChannel.invokeMethod('startAbility', {
    'bundleName': bundleName,
    'abilityName': data?['abilityName'],
    'parameters': data,
  });
  return result as bool? ?? false;
}
```

## 使用方式

### 检测鸿蒙平台

```dart
import 'package:miss_ide/platform/platform_adapter.dart';

// 检测平台类型
final adapter = PlatformAdapterFactory.instance;
if (adapter is OhosPlatformAdapter) {
  // 鸿蒙特定功能
  final apiVersion = await adapter.getHarmonyApiVersion();
}
```

### 调用平台功能

```dart
// 获取应用信息
final appInfo = await adapter.getAppInfo('com.example.app');

// 检查应用是否安装
final installed = await adapter.isAppInstalled('com.example.app');

// 打开外部应用
await adapter.openExternalApp('com.example.target', data: {
  'abilityName': 'MainAbility',
});
```

## 条件编译

Flutter 支持条件编译，可以针对不同平台编译不同代码：

```dart
// lib/platform/platform_adapter.dart
export 'platform_android.dart' if (dart.library.ohos) 'platform_ohos.dart';
```

## 外部工具集成

鸿蒙设备上的外部工具集成需要使用不同的机制：

1. **鸿蒙应用调用**
   - 使用 FA 模型启动目标应用
   - 通过 Ability 传递数据

2. **命令行执行**
   - 鸿蒙设备通常不提供 shell 访问
   - 考虑使用 HiLog 进行日志输出

## 已知限制

1. **Shell 执行**
   - 鸿蒙设备不提供标准 shell 环境
   - 部分命令行工具可能不可用

2. **第三方应用调用**
   - 需要目标应用支持 FA 模型
   - 部分应用可能不支持被外部调用

3. **文件访问**
   - 鸿蒙使用分布式文件系统
   - 权限模型与 Android 不同

## 测试建议

1. 使用鸿蒙模拟器进行开发测试
2. 在不同 API 版本（9-12）的设备上测试
3. 验证权限申请流程
4. 测试应用间跳转功能

## 参考资料

- [Flutter 官方鸿蒙支持](https://flutter.dev/harmonyos)
- [ArkUI 开发指南](https://developer.huawei.com/consumer/cn/doc/)
- [FA 模型开发指南](https://developer.huawei.com/consumer/cn/harmonyos/)
